{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (printf "%s-serviceaccount" .Release.Name) }}
  labels:
    app.kubernetes.io/name: system-process-exporter
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
